# 2026-07-04

## a11y: CRON schedule input missing aria-label (10:00 UTC)

**Issue found:** `AlertReportCronScheduler.tsx:108` (`superset-frontend/src/features/alerts/components/`) — the CRON expression text `<Input>` (shown when a user picks "CRON Schedule" in the Alert/Report scheduling modal) had no `aria-label`, `aria-labelledby`, or associated `<label>`. Only a placeholder (`t('CRON expression')`) plus a visually adjacent but structurally unassociated `<div className="control-label">` reading "Schedule". Screen reader users tabbing in heard no accessible name.

WCAG 2.1 SC 1.3.1 (Info and Relationships) / 3.3.2 (Labels or Instructions) — Level A.

Found via an Explore-agent sweep after checking `gh pr list --search "aria-label OR accessibility OR a11y"` (~50 open PRs, incl. #39230-#39245 broad sweeps, #40062, #37805, plus this week's own #41619/#41470/#41742) and ruling out several other candidates already touched by prior sessions (`FilterTitleContainer.tsx`, `ItemTitleContainer.tsx`, `DraggableFilter.tsx`, `VizTypeGallery.tsx` both `<img>` tags already have `alt`, `BaseIcon.tsx` systemic issue flagged out-of-scope). Confirmed no in-flight PR touches `AlertReportCronScheduler.tsx` or the CRON-schedule flow.

**Fix:** added `aria-label={t('Schedule')}` to the `<Input>` — 1-line diff. Matches the naming convention already used by the sibling `Select` above it (`ariaLabel={t('Schedule type')}`, which itself mirrors its own adjacent visible label). Confirmed `Input` is a bare re-export of antd's `Input`, which spreads unrecognized props (incl. `aria-label`) onto the underlying `<input>` DOM element — the prop reaches the DOM correctly. Confirmed single call site (`AlertReportModal.tsx:2674`) via grep.

Commit: `ebdfd2b8858` on `fix/a11y-cron-schedule-input-label` (off `origin/master`, pushed directly to apache/superset — same push-access account as recent days' fixes).

**PR:** https://github.com/apache/superset/pull/41769

## Self-review

Subsession `019f2c98-61b8-7f1f-ab5c-b16462d44f76` (callback to this session). **Verdict: approved, no changes needed.**
- Confirmed `aria-label` (not `aria-labelledby`) is the right call — the candidate label div contains a literal `*` required-indicator text node with no `aria-hidden`, so pointing `aria-labelledby` at it would make screen readers announce "Schedule asterisk"/"Schedule star", worse than the clean explicit string used.
- Confirmed the prop reaches the DOM (antd `Input.js` destructures a fixed prop list, not including `aria-label`, and spreads the rest onto the native `<input>`).
- Confirmed `t('Schedule')` (not `t('CRON expression')`) is the right string, per the file's own established pattern of matching the adjacent visible `control-label` div, not the placeholder.
- Completeness confirmed: single call site; `CronPicker` branch (the other schedule-type option) wraps a third-party library with its own internal labeling, not the same bug class, out of scope.
- **Flagged (non-blocking, pre-existing, not fixed):** the required-field `*` indicator throughout this file (and sibling modals) is a plain text node, never exposed via `required`/`aria-required` — screen reader users have no way to know these fields are mandatory. `CronPicker` itself has no `aria-label` of its own either (relies on `react-js-cron`'s internal labeling, unverified). Candidate for a follow-up ticket, not this narrow fix.

## Shortcut + Board

- **Story:** [sc-112664](https://app.shortcut.com/preset/story/112664) — state: Reviewing (500020186), epic: Automated Accessibility Fixes
- **Card:** `019f2c9c-0362-7e40-951d-bf586b5baf25` — SRE board "In Review" zone (`zone-pr-ready`)

## EngCodeReviewBot

Session: `019f2c9c-58f6-77d1-919f-e1f17a4da631`
URL: https://agor.sandbox.preset.zone/ui/s/019f2c9c58f677d1919fe1f1/
Branch: `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)
Callback → this session (`019f2c92-710c-7607-98d6-eb61dbff66e3`)
Status: **completed**

**Outcome:**
- Skipped independent AI review pass per "no AI review on own-authored PRs" convention (author eschutho/Elizabeth); confirmed via `gh pr view` the diff is genuinely trivial (1 file, +1/-0).
- **Reviewer rotation:** checked current load rather than trusting stale numbers — Kamil actually at 14 (not 15, #41681 merged today) but still highest on roster; Richard at 6 but already used this week for the identical pattern (#41742, merged this morning). Assigned **Rafael (rebenitez1802)** instead — a registered generalist at the lowest load among viable candidates (4→5), to stop the recurring a11y overflow stacking on the same one or two people. Good rotation call — worth watching whether Rafael becomes the new default a11y reviewer.
- Added via `gh pr edit --add-reviewer rebenitez1802`.
- **Notification deferred**: `frontend-build` CI was still `IN_PROGRESS` at assignment time, so per the no-notify-while-CI-pending convention, held off pinging Rafael in Slack (`<@U08MPC4Q2R5>`) until CI clears — will likely be caught by a heartbeat run.
- Created board card `♿ apache/superset#41769` in the bot's own `zone-in-review`; updated `team-roster.md`/`pr-tracker.md` (also fixed a small pre-existing drift where Rafael's Engineers-table and Load-Tracking-table counts disagreed, 5 vs 4).

**Follow-up for tomorrow's sweep:** confirm Rafael got pinged once CI cleared (bot deferred it, didn't send immediately).

## Follow-up candidates (not fixed today, out of scope)

- Required-field `*` text-node indicator pattern (`AlertReportCronScheduler.tsx` and sibling modals) is purely visual, never exposed via `required`/`aria-required` to assistive tech — repo-wide idiom, worth a dedicated ticket.
- `CronPicker` (wraps third-party `react-js-cron`) has no explicit `aria-label`/`aria-labelledby` tying it to the "Schedule" label — unverified whether the library's internal labeling is adequate; worth a spot-check independent of this PR.
- Carried over from 2026-07-03: `BaseIcon.tsx`'s `customIcons` branch (`packages/superset-ui-core/src/components/Icons/BaseIcon.tsx:71-101`) still silently ignores caller `aria-label` overrides for ~24+ icons in the `customIcons` list — high blast radius, still not picked up as a dedicated fix.

---

## Deprecation-warning fix: explore_json_data missing new_target hint (16:00 UTC)

**Warning found:** first result of Datadog `deprecat*` search (`now-1d`→`now`): `Superset.explore_json This API endpoint is deprecated and will be removed in version 5.0.0` (WARNING, `superset/views/base.py:186`'s `deprecated()` decorator, fired from the legacy `explore_json` view). Traced the message-generation code first — confirmed `explore_json` itself already got a `new_target="/api/v1/chart/data"` hint added in a prior merged PR (**#41159**, "add new_target to deprecated explore_json endpoints") and the once-per-process throttling from **#41286** (both already merged, both from prior sessions — 2026-07-01/07-02 logs). Also found **PR #41714** (open) doing a large-scale removal of the whole legacy explore_json/viz.py pipeline — deliberately avoided touching anything that PR's file list covers to prevent collision.

**Real gap found:** despite #41159's title saying "endpoints" (plural), its diff only touched `explore_json` — the sibling cache-polling route `explore_json_data` (`/explore_json/data/<cache_key>`, used by the async-explore job-polling flow, see `load_explore_json_into_cache` in `superset/tasks/async_queries.py`) still has a bare `@deprecated(eol_version="5.0.0")` with no migration hint. Confirmed `/api/v1/chart/data/<cache_key>` (`ChartDataRestApi.data_from_cache`, populated by the sibling `load_chart_data_into_cache` task in the same file) is the genuine modern equivalent — same cache backend (`cache_manager.cache`), same `generate_cache_key`/`set_and_log_cache` utilities, parallel async-job → cache-key → poll shape, not just a similarly-named endpoint.

**Fix:** added `new_target="/api/v1/chart/data/<cache_key>"` to `explore_json_data`'s `@deprecated()` decorator in `superset/views/core.py` — 1-line diff (refined to the parameterized path per self-review, see below). `uvx ruff check`/`ruff format` clean; pre-commit (mypy, pylint, ruff, blacklist, etc.) passed on commit.

**Commits:** `74667ec851d` (initial fix) + `e0aa6c16006` (self-review refinement) on `fix/deprecated-explore-json-endpoint` (off `origin/master`), pushed to `eschutho` remote.

**PR:** https://github.com/apache/superset/pull/41771

**Note on env:** no working local Python env for this repo (`uv run pytest` fails resolving deps — databricks/hive thrift version conflict; `/var/tmp/sup-venv` has a stale sqlglot causing an MRO error on import). Verified via `uvx ruff check`/`format` (works standalone) plus pre-commit's own hooks (mypy/pylint ran clean as part of `git commit`), and manual/self-review tracing instead of running pytest directly — consistent with what prior sessions' logs show (no evidence any of them got a live pytest run working here either).

### Self-review

Subsession `019f2de2-54b3-76e2-a419-134dfe544912` (callback to this session). **Verdict: approved, one non-blocking suggestion — acted on.**
- Traced full cache mechanics end-to-end (not just URL-prefix pattern-matching): confirmed same cache backend instance and same caching utilities power both the legacy and v1 async-result paths.
- Confirmed no existing constant/helper for the `/api/v1/chart/data` string to DRY against — already a bare literal at 3 call sites in `core.py`; introducing a shared constant now would be out of scope for a 1-line fix.
- Confirmed closure scoping: each `@deprecated(...)` call site gets its own independent `_warned` flag (traced `_deprecated`/`wraps` construction in `base.py`) — this fix doesn't share state with `explore_json`'s decorator instance.
- Confirmed only test touching this area (`test_explore_json_data_invalid_cache_key`) is unaffected; `test_deprecated_logs_warning_exactly_once` is generic/synthetic.
- Confirmed via `gh pr view 41714 --json files` — no overlap with `superset/views/core.py`.
- **Suggestion (non-blocking, acted on):** pointed out the codebase's own convention (`fetch_datasource_metadata`'s `new_target`) includes the route's path-param template for parameterized endpoints — tightened `new_target` from bare `/api/v1/chart/data` to `/api/v1/chart/data/<cache_key>` for consistency. Second commit, not amended.

### Shortcut + Board

- **Story:** [sc-112665](https://app.shortcut.com/preset/story/112665) — state: Reviewing (500020186), team: Automations
- **Card:** `019f2de5-4c08-7180-9f1a-4cfe4ae47219` — SRE board "In Review" zone (`zone-pr-ready`)

### EngCodeReviewBot

Session: `019f2de5-8474-77b3-ac44-105250a7df9a`
URL: https://agor.sandbox.preset.zone/ui/s/019f2de5847477b3ac441052/
Branch: `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)
Callback → this session
Status: **completed**

**Outcome:**
- Assigned reviewer: **Beto (betodealmeida)** — author is Elizabeth herself (excluded); Richard/Rafael both already used this week for the recurring a11y pattern (yesterday and today respectively); Kamil highest-loaded and frontend-focused (mismatch for this backend `views/core.py` change). Beto lowest-load among viable candidates (4→5), "APIs" listed as an expertise area — reasonable fit.
- Added as GitHub reviewer on PR #41771.
- No AI review pass — author's-own-PR convention.
- **Notification deferred**: several CI checks (`frontend-build`, `check-python-deps`, `test-load-examples`, `pre-commit`) still `IN_PROGRESS` at assignment time — held off pinging Beto (`<@U01B36NQT6E>`) in #eng-reviews per the no-notify-while-CI-pending convention. Bot asked whether to auto-follow-up once CI clears or check back first — **flagging for Elizabeth**: no response given yet, defaulting to "will catch it on a future heartbeat/sweep" per established pattern from other sessions this week.
- Created board card in bot's own `zone-in-review`; updated `pr-tracker.md`/`team-roster.md` (Beto's load 4→5).

**Follow-up for tomorrow's sweep:** confirm Beto got pinged once CI cleared on #41771 (same pattern as today's other PR, #41769/Rafael — two pending notifications to check now).

## Requested Changes sweep (Step 1-4, later in day)

Checked SRE board `zone-in-review` ("Requested Changes") — 0 cards pinned. No PRs waiting on reviewer feedback today.

Log: No PRs in Requested Changes today.

## In Review zone sweep (22:xx UTC)

Checked all 4 cards in `zone-pr-ready` ("In Review") on the SRE board.

- **PR #41470** (`fix(a11y): add aria-label to dashboard IconButton`, card `019f068f-074e-724b-8d54-fba14891c900`) — `reviewDecision: REVIEW_REQUIRED` (only activity: `bito-code-review` bot COMMENTED, doesn't count as a human decision). `mergeStateStatus: BLOCKED` on a `sharded-jest-tests (1)` failure, but per workflow that check only matters once a PR is `APPROVED` — still awaiting review. No action.
- **PR #41619** (`fix(a11y): add aria-label to cross-filter search icon`, card `019f1d29-a5a3-7306-93e1-4e548c8f3fe2`) — `reviewDecision: REVIEW_REQUIRED`, no reviews at all yet (Kamil assigned 2026-07-01 still hasn't reviewed — 3 days now, worth flagging). Still awaiting review. No action.
- **PR #41769** (`fix(a11y): add aria-label to CRON schedule input`, card `019f2c9c-0362-7e40-951d-bf586b5baf25`) — `reviewDecision: REVIEW_REQUIRED`, no reviews yet (Rafael assigned today via EngCodeReviewBot, notification deferred pending CI). Still awaiting review. No action.
- **PR #41771** (`fix(views): add new_target to deprecated explore_json_data endpoint`, card `019f2de5-4c08-7180-9f1a-4cfe4ae47219`) — `reviewDecision: APPROVED` (rusackas), `mergeStateStatus: CLEAN`, no failing checks. **Merged** via `gh pr merge 41771 --repo apache/superset --squash` (merge commit `3e152d9bb7a`). Moved Shortcut story [sc-112665](https://app.shortcut.com/preset/story/112665) to Merged/Done (500020392). Moved card to `zone-merged`.

**Result:** 1 PR merged (#41771), 3 still awaiting human review (#41470, #41619, #41769) — no CHANGES_REQUESTED/COMMENTED decisions today, so no EngCodeReviewBot escalations needed.

**Question for Elizabeth:** #41619 has been sitting on Kamil's plate with zero review activity for 3 days now (assigned 2026-07-01) despite the load-rotation fixes made since — worth a nudge or reassignment to whoever's more available?
