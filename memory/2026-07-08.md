# 2026-07-08

## Deprecation-warning sweep (16:15 UTC) — no PR opened, 3rd consecutive day blocked on the same item

**Task:** find one deprecation warning in prod Datadog logs, fix it, PR, self-review, card, EngCodeReviewBot, Shortcut story.

**Query:** `deprecat*`, `now-1d`→`now`. Top/most-frequent result, verbatim same as 07-06:

```
/home/superset/preset/preset/mcp/oauth_factory.py:20: AuthlibDeprecationWarning: authlib.jose module is deprecated, please use joserfc instead.
```

**Confirmed nothing has moved since the 07-06 escalation:**
- PR [preset-io/superset-shell#4091](https://github.com/preset-io/superset-shell/pull/4091) (code-level suppression) — still `CLOSED`, `updatedAt` still 2026-06-01. No new activity.
- PR [preset-io/superset-shell#4138](https://github.com/preset-io/superset-shell/pull/4138) (joserfc migration, temp CI test) — still `CLOSED`, same date.
- Amin's Slack question to the team ("suppress in Datadog vs suppress in code vs fix at root") posted 2026-05-27 — still no reply, 6+ weeks now.
- No new PRs referencing `authlib`, `joserfc`, `sc-107360`, or `ch107360` since.

**New this session — actually evaluated the real fix (joserfc migration) for feasibility, rather than just re-flagging the stalled suppression PR:**

Read the full usage of `JsonWebToken`/`ExpiredTokenError`/`JoseError` in `oauth_factory.py`. The decode/validate logic lives in `_lenient_verify_token` (lines ~474-543) and is genuinely security-sensitive:
- Deliberately raises `ExpiredTokenError` vs. generic `JoseError` so callers can't use error specificity as a verification oracle (explicit comment on this).
- Custom env-suffix issuer/audience matching for cross-workspace token isolation, replacing FastMCP parent's exact-equality check.
- `joserfc` has a different decode/validate API and exception hierarchy than `authlib.jose` — this is not a mechanical find-replace, it's a real migration of auth-critical code.

**Conclusion: not safe to push through as an autonomous same-day "minimal fix."** This needs a dedicated PR with human security review, not something I should do solo under today's task framing. Redoing #4091's suppression approach verbatim would also just recreate the exact stalled decision already escalated twice (07-01 implicitly, 07-06 explicitly).

**Other candidates checked and ruled out:**
- **Auth0: "Legacy Management of Connection's Enabled Clients... is being deprecated"** — new distinct message today. This is an Auth0 tenant/connection-level setting, not code. Checked `terraform-live-envs` for `auth0_connection`/`enabled_clients` resources — none exist. No IaC-managed path, no PR possible.
- **Datadog agent: `` `-config` argument is deprecated... use `--config` instead ``** — from the `datadog-agent` daemonset (`kube_cluster_name:koa-aws-mpc`). No filesystem access to the `helm` repo this session (permission denied — group not provisioned for me). Also almost certainly templated by the upstream Datadog Helm chart's default args, not something set directly in our values files even if I had access.
- **"Upserted user ... [PresetAlpha, Consumer Role (Deprecated User Level)]"** — grepped both `superset-shell` and `superset` for "Deprecated User Level" / "Consumer Role", zero hits. Same class as the Auth0 item above: an Auth0 tenant-side role/permission label, not our code.
- `appbuilder.app`, `sqlalchemy_redshift`/`pkg_resources`, Celery `CELERY_*`/`BROKER_*`, `explore_json` — all already conclusively ruled out 07-01/07-06 as stale-deploy artifacts or already-merged upstream fixes. Not re-litigating.

**No branch, no PR, no self-review, no card, no EngCodeReviewBot session, no Shortcut story today.**

## Recommendation for Elizabeth (stronger than 07-06's open-ended ask)

The `authlib.jose` Slack poll has been unanswered for 6+ weeks and this is now the 3rd sweep in a row landing on it with nothing to show. My honest take as senior engineer: waiting indefinitely for a poll that isn't getting answered is worse than shipping the small, reversible fix. Concretely:

- **PR #4091 (revive)** is already fully reviewed (2 automated LGTMs, addressed all Griffen/Codex feedback including the regex-escape fix and a `# TODO(sc-107360)` migration marker), trivially reversible, and scoped to exactly this warning. I'd lean toward reviving it as the pragmatic default — but it's Amin's authored work and a team-process question, so I'm not doing that unilaterally without your (or his) go-ahead.
- The **real fix (joserfc migration)** should be scheduled as dedicated work with human review given the security-sensitive decode/validate logic involved — not bundled into an autonomous daily sweep.
- Either way, **sc-107360 is currently marked "Deployed to Stable"/completed but the warning is still firing in prod** — that status is wrong and should get corrected regardless of which path you pick, so it stops giving false signal that this is resolved.

Want me to (a) revive #4091 under my own name/PR next sweep, (b) leave it fully alone until someone picks a direction, or (c) something else? I'll hold off touching this branch again until I hear back, and will keep picking a different warning (or reporting none available) on future sweeps rather than re-flagging this one daily.
