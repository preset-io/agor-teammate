# 2026-07-15

## 10:00 UTC — Daily a11y fix: CssTemplateModal Name label association (scheduled run)

**Search:** Checked memory logs 07-09 through 07-14 first — this week already covered RightMenu icon links, EmbedCodeContent, CopyToClipboard, ViewportControl/CrossFilterTitle icon aria-labels, dashboard IconButton, and TagModal Description label. `gh pr list --search "aria-label OR accessibility OR a11y" --state open` returned ~30 hits; pulled the full file manifest (`gh pr view <n> --json files`) across all of them — 357 files already touched.

First candidate from an Explore agent (`ActivityTable.tsx`'s `getEntityIcon` returning bare `Icons.*` components as card avatars) turned out to be a false positive on closer inspection: Superset's `Icons.*` wrapper (`BaseIcon.tsx`) auto-generates an `aria-label` + `role="img"`/`"button"` from the icon's filename via `genAriaLabel()` (e.g. `Icons.DashboardOutlined` → `aria-label="dashboard"`), so bare icon-only usage through that wrapper is not actually a violation. Worth remembering for future runs — don't flag plain `<Icons.X />` icon-only usage as broken; only antd `<Button icon={...} />` with no visible text *and* no explicit `aria-label` prop is suspect, since the outer button doesn't necessarily inherit the inner icon's label depending on antd's composition.

Ruled out further categories directly: zero raw `<input>` tags outside excluded files; only one file uses raw `<img>` (`VizTypeGallery.tsx`, already excluded/covered); the 4 files with `<div>/<span onClick>` outside excluded files were all already excluded too. Second Explore pass targeting unlabeled antd form inputs found `features/cssTemplates/CssTemplateModal.tsx`.

**Found:** `superset-frontend/src/features/cssTemplates/CssTemplateModal.tsx:250-259` — the "Name" field's visible label was a plain `<div className="control-label">`, not programmatically associated with the adjacent `<Input>` (no `htmlFor`/`id`, `aria-label`, or `aria-labelledby`). WCAG 2.1 SC 4.1.2 (Name, Role, Value), Level A. Confirmed via `gh pr list --search "CssTemplateModal OR CssTemplate"` (empty) that no open PR touches this file, and it wasn't in the 357-file exclusion manifest.

**Fix (2 files, isolated worktree `/tmp/superset-csstemplate`, main repo checkout at `/home/agorpg/.agor/repos/superset` left untouched — it has unrelated uncommitted work from someone else's `fix/pkg-resources-redshift-warning-suppression` branch):** changed the label `<div>` to `<label htmlFor="css-template-name">`, added `id="css-template-name"` to the `Input`, matching the existing `TagModal`/`DuplicateDatasetModal` pattern. Added `associates the Name label with its input` test to `CssTemplateModal.test.tsx` (`screen.findByLabelText('Name*')`). Ran the full test file — 4/4 passing.

**Branch/commit:** `fix/a11y-csstemplate-name-label`, off `origin/master`. Commit `de330b2615`.

**PR:** https://github.com/apache/superset/pull/42081

### Environment notes
- Fresh worktree needed `npm install` (not `npm ci` — same lockfile drift as prior days, missing `@noble/hashes@2.2.0`). `package-lock.json` diff reverted before committing.
- Pre-commit's `Type-Checking (Frontend)` hook failed on stale `packages/*/lib` `.d.ts` output (`superset-core`, `superset-ui-core`, `superset-ui-chart-controls`, `superset-ui-switchboard`) — same known pre-existing gap. Built each via `npx tsc --build` (not committed). Pre-commit then passed clean.
- `git push origin <branch>` worked directly on the first retry (after one transient "temporarily unavailable" classifier hiccup) — no need for a personal fork remote. Consistent with 07-14's finding that `eschutho` (the authenticated `gh` account) has direct write access to `apache/superset`.

## Self-review

Subsession `019f6547-6a66-7a26-bf4b-d5d7972b832d` (callback → this session). **Verdict: Approve**, one non-blocking suggestion — applied:
- Confirmed `htmlFor`/`id` is correct over `aria-label` (static form field, visible label renders fine) and matches the codebase's established pattern (`DuplicateDatasetModal.tsx`, `TagModal.tsx`, `SSHTunnelForm.tsx` all do the same).
- Confirmed antd's `Input` (`packages/superset-ui-core/src/components/Input/index.tsx:19`, a direct re-export of antd's `Input`) forwards `id` correctly to the native input.
- Confirmed no id-collision risk — `CssTemplateModal` is mounted exactly once (`CssTemplateList/index.tsx:263`), not per-row.
- Label text `'Name*'` confirmed to match rendered text.
- **Gap flagged (non-blocking):** the `css` field directly below (lines 261-274) has the identical bug — its label `<div>` isn't associated with the `StyledEditorHost` code editor. Scoping this PR to just `Name` is reasonable (smaller diff; the editor case likely needs `aria-labelledby` on the editor container rather than a simple `htmlFor`/`id` pair, so it may deserve its own PR) — **applied the suggestion** by calling this out explicitly as a known, intentional follow-up in the PR body rather than leaving it as a silent gap.

## Shortcut + Board

- **Story:** [sc-113477](https://app.shortcut.com/preset/story/113477) — state: Reviewing (500020186), team: Automations, epic: 111841 (Automated Accessibility Fixes)
- **Card:** `019f654d-4729-737f-9cbc-1cad973253a9` — SRE board "In Review" zone (`zone-pr-ready`)

## EngCodeReviewBot

Session: `019f654e-a6d2-7125-a5ca-0182494770aa`
URL: https://agor.sandbox.preset.zone/ui/s/019f654ea6d27125a5ca0182/
Branch: `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)
Callback → this session (`019f6538-3211-7199-a6f8-d5aff27eb0cd`)
Status: dispatched, awaiting callback (will append outcome once received — see follow-up note below if this log wasn't updated before session end).
