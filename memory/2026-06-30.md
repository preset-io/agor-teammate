# 2026-06-30

## sc-107864: Address PR review comments on #4246

PR: [preset-io/superset-shell#4246](https://github.com/preset-io/superset-shell/pull/4246)
Commit: `707a92a7`

### Changes made

**Comment 1 — ThreadPopoverContent useThreads mismatch:**
- Added `sortBy: 'most_recent_message_timestamp'` to the `useThreads` call that builds `threadChartIds`, matching `ThreadedComments`' sort order.
- Added `useEffect` to call `threadsInfo.fetchMore(100)` while `threadsInfo.hasMore && !threadsInfo.loading`, so dashboards with >10 threads get all visible threads in the fallback map.

**Comment 2 — CordUseHighlightedThreadTab reads useThread metadata (empty for non-authors):**
- `ThreadSource.tsx` onClick now appends `chartID=${chartId}` to URL alongside `threadID`.
- `CordUseHighlightedThreadTab.ts` reads `chartID` from URL search params as fallback when `useThread` returns no metadata.
- Updated `ThreadSource.test.tsx`: click assertion now expects `?threadID=thread-1&chartID=1`.
- Updated `CordUseHighlightedThreadTab.test.tsx`: converted static `useLocation` mock to dynamic (`mockSearch` variable); added test case for non-author tab navigation via `chartID` URL param.

Replied to both review comments on the PR.

## sc-111787: Broken DB documentation links in Create Database / Import modals

Ticket: [SC-111787](https://app.shortcut.com/preset/story/111787) (P3, customer-reported, Ribbon Ent)

**Bug:** Doc links in the Create Database modal + Import DB error alerts point to the old `superset.apache.org/docs/databases/*` namespace, now 404. Docs reorganized → `/user-docs/databases/*`.

**Triage findings (verified live via WebFetch):**
- ❌ `/docs/databases/installing-database-drivers` → 404
- ❌ `/docs/databases/postgres` → 404
- ✅ `/user-docs/databases/#installing-database-drivers` → 200
- ✅ `/user-docs/databases/supported/postgresql` → 200 (engine pages live under `/supported/<slug>`)
- `ModalHeader.tsx` `DOCUMENTATION_LINK` (~L36) was ALREADY fixed to `/user-docs/...` on master.

**Remaining broken production links (the fallback `: 'https://...'` branches):**
1. `ImportModal/ErrorAlert.tsx` ~L31
2. `ImportModal/ImportErrorAlert.tsx` ~L29
3. `ModalHeader.tsx` ~L59 engine-specific `/docs/databases/${engine}` → `/user-docs/databases/supported/${engine}` (engine→slug not 1:1; e.g. bigquery→google-bigquery — extend `irregularDocumentationLinks`).

**Orchestration:**
- Branch: `fix-db-docs-links-sc111787` (id `019f170f-019d-7dec-9598-f4c606f1bce1`), forked from master, board Supersetter, zone In Progress. issueUrl set.
- Worker session: `019f170f-a08b-7d69-8b6b-4babd7b2df06` ([link](https://agor.sandbox.preset.zone/ui/s/019f170fa08b7d698b6b4bab/)), callback enabled → me (`019f170b-00cb-72a7-82e2-a7199fabedab`).
- SC → Implementing, owner Sophie, team Buggy Pirates. Session link commented on ticket.
- Worker callback received: committed `26a81ed`. Diff clean, well-scoped (ImportModal x2 fallback URLs + ModalHeader engine path + irregular slug map; new ModalHeader.test.tsx + Footer.test mock). All slugs cross-checked against docs source.
- **PR opened: [apache/superset#41557](https://github.com/apache/superset/pull/41557)** (Dance B done). Pushed to preset-io/superset fork. Branch pullRequestUrl set; SC external link added; branch → zone-auto-review.
- B2 in progress: CI running (early checks green). Copilot reviewer add FAILED — `GITHUB_TOKEN_WRITE` lacks `read:org` scope for `--add-reviewer`. Copilot may auto-review anyway. zone-auto-review CI patrol (30-min) will continue polling + address bot feedback.
- No Slack thread reference for this ticket (came via direct SC link), so no Slack reply owed.
- CI Patrol flagged Jest shard 5 (`SavedQueryList.test.tsx`) + shard 7 (`TablePreview.test.tsx`) failures — both unrelated to PR changes (docs links). Confirmed flaky; logged to FLAKY_TESTS.md. Re-triggered run 28423531858. CI patrol will catch when green.
- Next: CI green + bot feedback addressed → move to QAgor (Dance B3).

## QAgor session-limit block — SC-111745, SC-111747×2 (2026-06-30)

All 3 QAgor sessions created for the June-26 CI-green PRs hit the QAgor account rate limit:
- SC-111745 (PR #41462): session `019f0595-fb8c` — "session limit · resets 9:40pm UTC"
- SC-111747 star fix (PR #41460): session `019f0596-064d` — same
- SC-111747 filter (PR #41461): session `019f0596-124d` — same

All 3 completed with 0 tool uses (hit limit immediately). Cards already created in QAgor intake zone, branches in zone-qagor, SC tickets in Reviewing.

**Action needed after 9:40pm UTC:** Retry all 3 QAgor sessions by creating new sessions on QAgor branch `8114e2cf-b0eb-43dd-8468-22f653d466c0` with callbacks to current orchestrator session.

## Merge patrol — 2026-06-30

Scanned 13 board branches with a `pull_request_url` outside `zone-done`. **2 PRs merged, both actioned:**

- **[apache/superset#41462](https://github.com/apache/superset/pull/41462)** (merged 2026-06-30) — [SC-111745](https://app.shortcut.com/preset/story/111745) BigQuery drill-to-detail STRUCT fix. Ticket already in **Merged/Done** (left as-is). Branch `019f0568-5cd2…` → `zone-done`, env stopped.
- **[apache/superset#41460](https://github.com/apache/superset/pull/41460)** (merged 2026-06-29) — [SC-111747](https://app.shortcut.com/preset/story/111747) Tags favorite-star fix. Branch `019f0568-6bce…` → `zone-done`, env stopped. **Ticket left in Reviewing on purpose:** SC-111747 covers two deliverables (star fix + favorites filter) and the filter PR **[apache/superset#41461](https://github.com/apache/superset/pull/41461)** is still **OPEN**. Will move the story to Merged/Done once #41461 also merges.

**Slack replies owed** (no Slack tool in scheduled runs — human to post in each PR's original thread):
- "Merged ✓ https://github.com/apache/superset/pull/41462" — SC-111745 thread
- "Merged ✓ https://github.com/apache/superset/pull/41460" — SC-111747 thread

The other 11 candidates were OPEN (9) or CLOSED-not-merged (1, [#39254](https://github.com/apache/superset/pull/39254)) — no action.
