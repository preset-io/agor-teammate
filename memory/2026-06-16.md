# 2026-06-16 — Deprecation Warning Fix Session

## ~16:15 UTC — Deprecation warning found (Datadog)

**Query:** `deprecat*` service:superset environment:production, now-24h

**Signal identified:** `FutureWarning: Series.__getitem__ treating keys as positions is deprecated` at `helpers.py:1956` in production (31 `appbuilder.app` + 4 `Series.__getitem__` in last 24h).

All high-frequency warnings (`appbuilder.app`, `Series.__getitem__`) are already fixed in apache/superset master (commits `c0e78f39d7`, `be71b48082`, `b0d7880ac0`). Production image is running old code (`48e588793229f5...`).

**Fix chosen:** `np.NaN` → `np.nan` at `superset/models/helpers.py:1950` — deprecated since NumPy 1.24, removed in NumPy 2.0, still present in current master. Production runs NumPy 1.26.4 where this emits a `DeprecationWarning`. Fix is minimal and correct.

---

## Fix

**Repo:** apache/superset  
**Branch:** `fix/np-NaN-deprecated`  
**Commit:** `6230880b69`

Changes:
- `superset/models/helpers.py:1950`: `np.NaN` → `np.nan` (placeholder DataFrame in `processing_time_offsets` when offset query returns no rows)
- `tests/unit_tests/pandas_postprocessing/test_resample.py:256`: `np.NaN` → `np.nan` (4 occurrences in test fixture)

All pre-commit hooks passed (auto-walrus, mypy, ruff, pylint, blacklist, ruff-format).

---

## Self-Review

Subsession: `019ed139-2879-7a5d-b441-8a3b4bfd035a`  
URL: https://agor.sandbox.preset.zone/ui/s/019ed13928797a5db4418a3b/  
Status: **COMPLETE**

Findings:
1. **Correctness** ✅ — `np.NaN` and `np.nan` are both `float('nan')`. Functionally identical. Safe.
2. **Completeness** ✅ — grep confirms zero remaining `np.NaN` in any `.py` file. Fix is exhaustive.
3. **Modeling/placement** ✅ — `pd.DataFrame({col: [np.nan] ...})` is idiomatic pandas for an empty-but-schema-correct placeholder. Right place.
4. **Test coverage** ⚠️ FLAG (non-blocking) — `test_resample.py:256` and `helpers.py:1950` are independent (resample tests != processing_time_offsets). The empty-offset branch in `processing_time_offsets` has no dedicated unit test — pre-existing gap, not introduced by this PR.
5. **Lambda/closure** — N/A

No action needed. Good to merge.

---

## PR

**URL:** https://github.com/apache/superset/pull/41118  
**Title:** fix(deps): replace deprecated np.NaN with np.nan

---

## Board Card

Card ID: `019ed139-b356-7a14-a7fa-98ba68ee212a`  
Zone: `zone-pr-ready` on SRE board (9a49d11a-6605-4581-b58e-f45fe6bfbadb)

---

## EngCodeReviewBot

Session: `019ed139-f26c-765c-9ab0-63012b5a2b74`  
Branch: `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)  
URL: https://agor.sandbox.preset.zone/ui/s/019ed139f26c765c9ab06301/  
Status: **COMPLETE**

Reviewer: **Beto (betodealmeida)** — pandas post-processing domain expert; best match for `test_resample.py` and `processing_time_offsets`.

- Beto added as GitHub reviewer on apache/superset#41118
- Roster updated: Beto active reviews 2→4 (counting superset-shell#4243 + this PR)
- No Agor agent for Beto; GitHub assignment is the notification
- Amin OOO until 2026-06-27, Rafael OOO until 2026-06-22, Antonio on-request-only

---

## Shortcut Story

**ID:** sc-111178  
**URL:** https://app.shortcut.com/preset/story/111178  
Team: Automations, type: chore, state: Reviewing (500020186)
