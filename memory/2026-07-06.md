# 2026-07-06

## Deprecation-warning sweep (17:00 UTC) — no new PR opened, flagged to Elizabeth instead

**Task:** find one deprecation warning in prod Datadog logs, fix it, PR, self-review, card, EngCodeReviewBot, Shortcut story.

**Query:** `deprecat*`, `now-1d`→`now`, `env:production`. First result (and most frequent, 6 occurrences/day):

```
/home/superset/preset/preset/mcp/oauth_factory.py:20: AuthlibDeprecationWarning: authlib.jose module is deprecated, please use joserfc instead.
```

Real, unfixed, in `preset-io/superset-shell` (not `apache/superset` — confirmed `oauth_factory.py` only exists in superset-shell; today's task instructions assumed the OSS repo, which was wrong for this warning). Confirmed still present verbatim on `origin/master` today (no suppression, no joserfc migration).

**Did not open a new PR. Here's why:**

This exact warning already went through a full cycle in May/June and stalled on an unresolved team question, not a technical gap:
- **PR [preset-io/superset-shell#4091](https://github.com/preset-io/superset-shell/pull/4091)** — code-level `warnings.catch_warnings()` suppression around the `oauth_factory.py` import, scoped and reviewed (2 LGTMs from automated reviewers, `04b1977f6f` addressed all review feedback including a documented note that `fastmcp`'s own `authlib.jose` import isn't covered). **Closed without merging** — author (Amin) explicitly asked the team to choose between "Datadog-level exclusion" vs. "code-level suppression" before merging, posted to Slack 2026-05-27, and got no reply. Nobody ever decided; the PR just sat and was closed.
- **PR [preset-io/superset-shell#4138](https://github.com/preset-io/superset-shell/pull/4138)** — parallel attempt at the *real* fix (migrate to joserfc via `apache/superset#40582`, which **did** merge upstream for `jwt_verifier.py`'s error imports). Closed as "temporary/test-only," never finished for `oauth_factory.py` itself.
- Shortcut story **[sc-107360](https://app.shortcut.com/preset/story/107360)** tracking this is marked **completed / "Deployed to Stable"** (2026-06-03) — which is stale/wrong: the warning is still firing in prod today, verified against current `origin/master`. Looks like the story got closed when `apache/superset#40582` merged, without confirming `oauth_factory.py`'s own copy of the warning (the actual thing generating today's log noise) was also resolved.

Redoing the same suppression PR under my own name would just recreate the exact same stalled decision rather than resolve it. Flagging to Elizabeth instead of guessing which direction the team wanted.

**Other results checked, all ruled out (matches 2026-07-01's independent findings — same conclusions reached again today):**
- `appbuilder.app is deprecated` (7×) — already fixed upstream (`apache/superset#40876`, `preset-io/superset-shell#4282`, both merged). Still firing because deployed image (`ee43906d39`, dated 2026-06-23) predates the June 30 fix — a stale-deploy artifact, not a code gap. Same conclusion as 7/1 (that time it was ~3 weeks stale; still stale today).
- `The field \`groupby\` is deprecated` (6×) — `query_object.py:219`, already fixed via `apache/superset#41204` (merged 2026-06-18); customer traffic (wealthsimple) still hitting old cached `query_context` JSON predating the fix. Not a code gap.
- `sqlalchemy_redshift` `pkg_resources` `UserWarning` — already fixed, merged 2026-07-02 (`apache/superset#41691`).
- `Superset.explore_json` deprecated endpoint — intentional, registry-gated legacy-viz-plugin migration (SIP-scale), not a minimal-fix candidate (ruled out 2026-06-17/07-01).
- Celery `CELERY_*`/`BROKER_*` uppercase-config warnings — already fixed, merged 2026-06-19 (`preset-io/superset-shell#4229`); residual firing looks like other per-workspace overrides, same class already addressed.

**No branch, no PR, no card, no EngCodeReviewBot session, no Shortcut story today** — nothing safe to attach them to without Elizabeth's call on the stalled #4091 decision. Asked her directly in-session: revive #4091 (I'd rebase + push + open fresh), pursue the full joserfc migration for `oauth_factory.py` instead, or leave it (and if so, should sc-107360 get reopened since it's marked done but isn't)?

**Recurring observation worth escalating separately:** this is the second time (7/1, today) the `superset-private` prod image has been caught running ~1-3 weeks stale relative to `origin/master` merged fixes. Not filing a ticket for it unprompted, but flagging the pattern.

## a11y: Google Sheets catalog "remove sheet" icon missing aria-label (10:00 UTC)

**Issue found:** `TableCatalog.tsx:73` (`superset-frontend/src/features/databases/DatabaseModal/DatabaseConnectionForm/`) — the `Icons.CloseOutlined` icon shown next to each Google Sheet entry (when 2+ sheets are configured on a database connection) had an `onClick` handler removing that catalog entry, but no `aria-label`/`aria-labelledby`. `BaseIcon.tsx` auto-generates a fallback `aria-label` from the icon's filename ("close"), so screen reader users heard a generic, context-free announcement rather than knowing what the button removes.

WCAG 2.1 SC 4.1.2 (Name, Role, Value) — Level A.

Found via an Explore-agent sweep after checking `gh pr list --search "aria-label OR accessibility OR a11y"` (~30 open PRs, all either already-known in-flight a11y PRs from this week or unrelated features) and confirming no open PR touches `TableCatalog.tsx` or this catalog flow. Ruled out a second candidate (`AlertReportModal.tsx`'s filter-delete `Icons.DeleteOutlined`, line ~2593) in favor of this one — cleaner single call site, no wrapping `<div>`.

**Note on repo state:** found an orphaned local commit already sitting on a stale branch (`fix/a11y-option-remove-button`, aria-label fix for Explore option-control remove buttons, WCAG 4.1.2, authored `eschutho`, dated 2026-07-05, never pushed/PR'd, no memory trail) from what looks like an interrupted prior run. Did not touch it — rebasing/pushing a branch whose commits I didn't create this session was correctly blocked by the auto-mode safety classifier as a "rewrite pre-existing work" risk, and it isn't safe to assume ownership of unlogged work in a shared repo clone. Left it alone; **flagging for Elizabeth** — worth a look to decide whether to rescue (rebase + push + PR) or discard.

**Fix:** added `aria-label={t('Remove sheet')}` to the `Icons.CloseOutlined` element — 1-line diff. Matches the existing "Remove {noun}" convention already used by `FilterTitleContainer.tsx`'s `t('Remove filter')`. Confirmed `CloseOutlined` resolves through the plain-antd branch of `BaseIcon.tsx` (not `customIcons`), and since `aria-label` isn't destructured out before the prop spread, the explicit label correctly overrides the auto-generated fallback at the DOM level. Confirmed single call site (`DatabaseConnectionForm/index.tsx`) via grep. Pre-commit (prettier, oxlint, custom rules, frontend type-check) passed clean on commit.

**Commit:** `d3de92922d0` on `fix/a11y-table-catalog-remove-sheet` (off `origin/master`), pushed to apache/superset.

**PR:** https://github.com/apache/superset/pull/41796

## Self-review

Subsession `019f36e6-a4e1-79cb-a675-17d6aee5575b` (callback to this session). **Verdict: approved as-is, no changes needed.**
- Confirmed `aria-label` (not `aria-labelledby`) is correct — no existing visible text element to reference, and `whatRole` resolves to `role="button"` since `onClick` is present, so the label correctly names a button rather than an image.
- Confirmed prop-spread order in `BaseIcon.tsx`: `aria-label` lands in `rest`, which spreads last, so the caller's explicit label wins over the auto-generated one.
- Confirmed wording convention matches `FilterTitleContainer.tsx:214`'s identical `t('Remove filter')` pattern.
- Completeness confirmed: single call site; the only other actionable element in the file ("+ Add sheet") already has visible text, no icon-only gap.
- **Flagged (non-blocking, pre-existing, not fixed):** the icon gets `role="button"` automatically but no `tabIndex`/`onKeyDown` — not keyboard-operable. Same gap exists in `FilterTitleContainer.tsx`'s `DeleteOutlined` pattern — broader codebase issue, candidate for a follow-up ticket, out of scope for this narrow fix.

## Shortcut + Board

- **Story:** [sc-112669](https://app.shortcut.com/preset/story/112669) — state: Reviewing (500020186), team: Automations, epic: Automated Accessibility Fixes
- **Card:** `019f36e9-ff16-7698-816f-44704bf3549d` — SRE board "In Review" zone (`zone-pr-ready`)

## EngCodeReviewBot

Session: `019f36ed-8aeb-7594-becc-fb0145283b13`
URL: https://agor.sandbox.preset.zone/ui/s/019f36ed8aeb7594beccfb01/
Branch: `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)
Callback → this session
Status: **completed**

**Outcome:**
- Confirmed via `gh pr view` that author is eschutho (Elizabeth) — no independent AI review pass per the standing own-PR convention.
- **Reviewer:** assigned **Beto (betodealmeida)**, not the usual a11y-rotation trio (Richard load 6 and Rafael load 4 both already used this exact aria-label pattern on 7/3 and 7/4; Kamil at load 13 is the most overloaded on the roster). Beto's listed "database connectivity" expertise matches this component's location (`DatabaseConnectionForm`), and he's already reviewed related PRs (#40466, #41691) in that area — good targeted fit outside the usual pool.
- Added via `gh pr edit --add-reviewer betodealmeida`.
- **Notification deferred**: CI still heavily in progress (cypress, playwright, jest shards, lint-frontend, docker-build, storybook) — held off pinging Beto (`<@U01B36NQT6E>`) per the no-notify-while-CI-pending convention. Will likely be caught by a heartbeat run.
- Created board card in bot's own `zone-in-review`; updated `team-roster.md`/`pr-tracker.md`.

**Follow-up for tomorrow's sweep:** confirm Beto got pinged once CI cleared on #41796.

## Follow-up candidates (not fixed today, out of scope)

- Icon-only "remove" buttons with `role="button"` (auto-applied by `BaseIcon.tsx` when `onClick` is present) are not keyboard-operable — no `tabIndex`/`onKeyDown`. Seen in both `TableCatalog.tsx` (today) and `FilterTitleContainer.tsx` (pre-existing) — repo-wide idiom, worth a dedicated ticket.
- Orphaned unpushed commit on `fix/a11y-option-remove-button` (Explore option-control remove buttons, WCAG 4.1.2, dated 2026-07-05, no memory trail) — flagged above, needs an operator decision on whether to rescue or discard.
- Carried over from 2026-07-04: `BaseIcon.tsx`'s `customIcons` branch (`packages/superset-ui-core/src/components/Icons/BaseIcon.tsx`) still silently ignores caller `aria-label` overrides for icons routed through that branch — still not picked up as a dedicated fix.

## Question for Elizabeth

Found a completed, unpushed, un-logged commit (`fix/a11y-option-remove-button`, author eschutho, dated yesterday) sitting in the shared superset repo clone — looks like an interrupted prior run that never got to PR. I left it alone rather than rebase/push it myself (blocked by the safety classifier as rewriting pre-existing work, correctly IMO since I have no record of authoring it this session). Want me to pick it up as a follow-up PR next run, or should it be discarded?

## Requested Changes zone sweep (later run)
Checked SRE board (`9a49d11a-6605-4581-b58e-f45fe6bfbadb`), zone `zone-in-review` ("Requested Changes"). No cards present. No PRs in Requested Changes today.

## In Review zone sweep (later run)

Checked all 3 cards in `zone-pr-ready` ("In Review") on the SRE board.

**PR [apache/superset#41470](https://github.com/apache/superset/pull/41470)** (card `019f068f-074e-724b-8d54-fba14891c900`, sc-111842) — "fix(a11y): add aria-label to dashboard IconButton and unlabeled call sites". `reviewDecision: REVIEW_REQUIRED`, `mergeStateStatus: BLOCKED`, only activity is a `bito-code-review` bot comment (one actionable suggestion: hardcoded 'delete' string in a test assertion, non-blocking). No human review decision yet. **No action — still awaiting review.**

**PR [apache/superset#41619](https://github.com/apache/superset/pull/41619)** (card `019f1d29-a5a3-7306-93e1-4e548c8f3fe2`, sc-112103) — "fix(a11y): add aria-label to cross-filter search icon". `reviewDecision: REVIEW_REQUIRED`, no reviews at all yet. **No action — still awaiting review.**

**PR [apache/superset#41796](https://github.com/apache/superset/pull/41796)** (card `019f36e9-ff16-7698-816f-44704bf3549d`, sc-112669) — "fix(a11y): add aria-label to remove-sheet icon button in Google Sheets catalog". Already **MERGED** (merge commit `06f421ed4af18e0834f65d75c883c6ab548957d5`, 2026-07-06T17:38:42Z) — approved and merged by **rusackas** (apache/superset maintainer), not the Beto reviewer I'd added via EngCodeReviewBot earlier today. Merge happened upstream before my sweep got to it, so no `gh pr merge` needed.
- Moved sc-112669 from Reviewing (500020186) → **Merged/Done** (500020392).
- Moved card `019f36e9-ff16-7698-816f-44704bf3549d` from `zone-pr-ready` → `zone-merged`.
- Logged: merged PR #41796, moved card and Shortcut story to done.

**No questions for the operator from this sweep** — the two open PRs are simply waiting on human reviewers; nothing blocked on our side.
