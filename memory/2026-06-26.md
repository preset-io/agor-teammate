# 2026-06-26

## Merge patrol

Checked 10 candidate branches on the Supersetter board (have `pull_request_url`, not in `zone-done`, not archived) against real PR state via `gh pr view`.

**merge patrol: nothing to action** — no PRs merged since the last patrol.

States observed:
- [#39254](https://github.com/apache/superset/pull/39254) — CLOSED (not merged); branch in CANNOT REPRO zone, left alone.
- [#39506](https://github.com/apache/superset/pull/39506), [#40978](https://github.com/apache/superset/pull/40978), [#41106](https://github.com/apache/superset/pull/41106), [#41194](https://github.com/apache/superset/pull/41194), [#41314](https://github.com/apache/superset/pull/41314), [#39344](https://github.com/apache/superset/pull/39344), [#41101](https://github.com/apache/superset/pull/41101), [#40781](https://github.com/apache/superset/pull/40781), [#41100](https://github.com/apache/superset/pull/41100) — all still OPEN.

No Shortcut updates, no zone moves, no Slack replies owed.

## SC-111332 — Time Column control → single data point (dispatched)

Sophie handed me [SC-111332](https://app.shortcut.com/preset/story/111332) (P3, customer: The Knot). Bug: with Time Comparison (time offset) configured, changing the Time Column to a non-default temporal column collapses that series into a single data point. Reporter's key constraint: the Time Column can be set from **both** the Filters panel and the Display Controls — fix must work from both (both feed `extra_form_data` → override `granularity_sqla`).

- Branch: `sc-111332-time-column-single-point` (`019f0561-ecb6-7f86-a5e5-4b82e3a4e252`), forked from master, issueUrl set, moved to zone-in-progress.
- Worker session: `019f0562-681e-79ad-82f1-4eae63f8903b` — https://agor.sandbox.preset.zone/ui/s/019f0562681e79ad82f14eae/ (callback enabled).
- Suspected root cause given to worker: time-offset join in `superset/common/query_context_processor.py` likely joins offset df on the *default* temporal column instead of the *overridden* one from extra_form_data.
- SC → Implementing (500020185), session link commented on ticket.
- No Slack thread (handed via SC link directly), so no Slack reply owed.
- **Worker #1 (`019f0562-681e-...`) FAILED** at 19:38 — "Executor heartbeat lost; executor crashed/disconnected." 0 tool uses, ran ~15min before crash. Infra blip (agor MCP also disconnected/reconnected same time), NOT code. Branch/clone intact.
- **Retry — worker #2:** `019f0571-43bf-7260-9912-fe19d92e530b` — https://agor.sandbox.preset.zone/ui/s/019f057143bf72609912fe19/ (callback enabled, same prompt). New session link commented on SC ticket.
- **Worker #2 ALSO FAILED** at 19:54 — identical "Executor heartbeat lost", 0 tool uses, ~927s. NOT branch-specific: SC-111745 worker #1 hit the SAME crash in the SAME window and its retry succeeded. Intermittent env-wide executor instability; SC-111332 just got unlucky twice.
- **Retry — worker #3:** `019f0580-cffc-76cb-8e93-5cdbfdc21dc2` — https://agor.sandbox.preset.zone/ui/s/019f0580cffc76cb8e935cdb/ (callback enabled). Added note in prompt to commit incrementally so progress survives a blip.
- **Worker #3 ALSO FAILED** at 20:11 — identical "Executor heartbeat lost", 0 tool uses, ~933s. THREE-IN-A-ROW, all ~15.5min (929s/928s/933s), all 0 tool uses. This is a reproducible executor-bringup failure killed by a ~15.5min watchdog, NOT bad luck.
- Diagnostics: `agor_environment_health` → `filesystem_status: ready` (clone is fine), docker env `status: unknown` (never started — workers don't need it). So the FILESYSTEM/clone is good; the **claude-code executor process** never comes up. Env logs irrelevant (env never started).
- **STOPPED at 3 retries — ESCALATED to Sophie.** Did NOT retry #4. Branch left intact in zone-in-progress, ticket in Implementing. Options put to Sophie: (a) wait for infra to recover + retry, (b) recreate branch with `storage_mode: worktree` instead of `clone` (only obvious diff vs sibling tickets that recovered — UNVERIFIED hypothesis), (c) infra/human look at the agent-runner host, (d) I do read-only root-cause analysis myself meanwhile to hand the next worker a precise patch spec.

## SC-111747 — Tags favorite star + favorites filter (split into TWO PRs, dispatched)

Sophie handed me [SC-111747](https://app.shortcut.com/preset/story/111747) (P3, customer: Ribbon). Two distinct problems, split per Sophie's request into two separate PRs both linked to the same SC ticket:

**PR1 — Bug: favorite star non-functional** (branch `sc-111747-tags-favorite-star-fix`, `019f0568-6bce-7323-91df-dafdb82bc9f5`)
- Worker session `019f0569-3363-7d84-adb9-319973f1ec15` — https://agor.sandbox.preset.zone/ui/s/019f056933637d84adb93199/ (callback enabled).
- Root cause (confirmed via Explore): in `superset-frontend/src/pages/Tags/index.tsx` the `columns` useMemo (~line 260) is MISSING `saveFavoriteStatus` and `favoriteStatus` in its dep array → stale closure, FaveStar cell never reflects/toggles status. ChartList's columns useMemo includes them. Backend tag favorite endpoints already work.

**PR2 — Feature: favorites filter** (branch `sc-111747-tags-favorites-filter`, `019f0568-7197-77ed-a3e9-a9411dada0f9`)
- Worker session `019f0569-81b3-74f5-90e7-6641e9b065cf` — https://agor.sandbox.preset.zone/ui/s/019f056981b374f590e76641/ (callback enabled).
- Follow Charts/Dashboards CRUD pattern + ordering (Favorite after Owner, before Certified). Frontend: add `favoritesFilter` to Tags filters (guarded by userId) + `TagIsFav='tag_is_favorite'` operator in ListView/types.ts. Backend: new `TagFavoriteFilter` in `superset/tags/filters.py` registered under `"id"` in `tags/api.py` search_filters.
- **Critical gotcha given to worker:** tag favorites live in `user_favorite_tag_table` (M2M), NOT in `FavStar`. So TagFavoriteFilter must NOT subclass `BaseFavoriteFilter` (queries FavStar) — needs custom `apply()` mirroring `TagDAO.favorited_ids`.

- Both branches forked from master, issueUrl set, moved to zone-in-progress. Session links commented on the ticket. SC already in Implementing, owned by Sophie. No Slack thread (handed via SC link).

**BOTH WORKERS DONE → BOTH PRs OPEN (2026-06-26):**
- PR1 (bug fix): [apache/superset#41460](https://github.com/apache/superset/pull/41460) — commit `cc1012af17`. Worker proved the new Tags.test.tsx regression guard fails on revert / passes with fix. Ran oxlint clean.
- PR2 (filter): [apache/superset#41461](https://github.com/apache/superset/pull/41461) — commit `c1e764bf88`. eslint/prettier + integration test NOT runnable in worktree (no node_modules / no superset runtime) → **CI is the real check** on those.
- Both: pushed to preset-io fork, pullRequestUrl set on branch, added as SC external links, bodies cross-reference each other, moved to **zone-auto-review**.
- Gotcha logged: `gh pr edit` fails with this token (org-scope GraphQL); use `gh api -X PATCH repos/.../pulls/<n> -F body=@file` to edit PR bodies instead.
- NEXT (Dance B2): poll CI green on both, address bot reviewer feedback → QAgor (Dance B3). No Slack reply owed.

## SC-111745 — Drill to Detail on BigQuery fails on nested STRUCT column (dispatched)

Sophie handed me [SC-111745](https://app.shortcut.com/preset/story/111745) (P3, customer: Medialabgroup PCS-9996, High sev/High impact). Bug: Drill to Detail on a chart over a **physical** BigQuery dataset fails with a 400 when a column is a nested STRUCT registered with a dotted column name (e.g. `forecasts.original.total_cost`). Chart renders fine; only Drill to Detail fails.

- **Root cause** (scoped via Explore scout, given to worker): the normal chart query path quotes each path segment separately (`` `forecasts`.`original`.`total_cost` ``) which BigQuery resolves, but the drill-to-detail / samples path emits the whole dotted path as ONE quoted identifier (`` `forecasts.original` ``) → `Unrecognized name`. Divergence is in `superset/models/helpers.py` `get_sqla_query` (filter/column lookup ~3486-3541 vs per-segment quoting ~3357) vs `TableColumn.get_sqla_col` in `superset/connectors/sqla/models.py` (~1089-1114). Drill entry point: `superset/views/datasource/utils.py` `get_samples()`. Existing tests: `tests/integration_tests/datasource_tests.py::test_get_samples_with_filters`.
- Branch: `sc-111745-bigquery-drill-struct` (`019f0568-5cd2-74dd-bcec-935f5de663ad`), forked from master, issueUrl set, moved to zone-in-progress.
- SC → Implementing (500020185), session link commented on ticket. No Slack thread (handed via SC link), so no Slack reply owed.
- **Worker #1 (`019f0568-be36-...`) FAILED** at 19:45 — "Executor heartbeat lost; executor crashed/disconnected." 0 tool uses. SAME infra blip that killed SC-111332 worker #1 in the same window (agor MCP also disconnected/reconnected). NOT code. Branch/clone intact (worker #2 picked up a real base_sha).
- **Retry — worker #2:** `019f0577-a618-7821-90b6-6cf5907342e3` — https://agor.sandbox.preset.zone/ui/s/019f0577a618782190b66cf5/ (callback enabled, same prompt). New session link commented on SC ticket.

**WORKER #2 DONE → PR OPEN (2026-06-26):**
- Fix commit `ebf690a79f`. Interesting: worker #1 actually wrote the fix+test to the working tree before its heartbeat was lost (so it DID work, just lost the executor) — worker #2 found the draft, verified it against the real BigQuery dialect, fixed a mypy issue, ran tests, committed.
- **Fix:** `superset/models/helpers.py` `ExploreMixin.get_sqla_query` — in the `elif columns:` branch, short-circuit known columns (`selected in columns_by_name`) to `convert_tbl_column_to_sqla_col` (same helper chart path uses) before the `quote()`/sqlglot path. Dotted STRUCT name now quotes per-segment. Adhoc/unknown/virtual unaffected.
- **Test:** `tests/unit_tests/models/helpers_test.py::test_get_sqla_query_dotted_struct_column_bigquery` — real BigQuery dialect, asserts `\`forecasts\`.\`original\`.\`total_cost\`` present + `\`forecasts.original\`` absent. 73 tests in file pass; pre-commit clean.
- **PR: [apache/superset#41462](https://github.com/apache/superset/pull/41462)** — pushed to preset-io fork, pullRequestUrl set on branch, added as SC external link, PR-up comment on ticket. Moved to **zone-auto-review**.
- Copilot review requested (REST: `gh api -X POST repos/apache/superset/pulls/41462/requested_reviewers -f "reviewers[]=copilot-pull-request-reviewer[bot]"` — works; bot handle resolves to "Copilot").
- CI in progress, no early failures (lint-check/changes/translations/extensions-cli pass; docker/cypress/CodeQL pending). CI patrol (every 30 min on zone-auto-review) will continue polling.
- No Slack thread (handed via SC link), so no Slack reply owed.
- NEXT (Dance B2): poll CI green, address Copilot/bot feedback → QAgor (Dance B3).

**Bot review (2026-06-26):** essentially clean.
- Copilot: COMMENTED (not changes-requested). One inline nit at helpers.py:3360 — the code-comment example `\`a.b\`.\`c\`` is misleading (not a single identifier). Bito agreed. Codecov: all modified lines covered, 64.52% project.
- Bito: 0 actionable suggestions; MyPy/Ruff/secret scans all pass.
- Fixed the comment (commit `844695acb0`, comment-only — reworded to describe merged single-identifier form). Replied on Copilot inline thread. Pushed; CI re-running.
- No other valid points. Once CI green → QAgor (Dance B3).

**Status check 2026-06-30 (Max asked re: latest comment + QAgor):**
- Latest PR comment: Bito re-review (Run #a8a931, 06-30 00:26) — **0 actionable suggestions**, clean. CI now **fully green** (all heavy jobs pass). Codecov: 1 partial branch line (83.33% patch), non-blocking; codecov check passes.
- **QAgor had NOT run.** Card `019f0595-74d6-7dea-9b74-9a76b979fafe` ("SC-111745", PR #41462) was created in QAgor intake 06-26 20:18 and branch moved to zone-qagor, but the QAgor session was never kicked off (lost in 06-26 infra churn). Card never moved to pass/fail (updated_at==created_at); no QA verdict comment on ticket.
- **Kicked off QAgor now:** session `019f16eb-93e0-734a-ae9c-0791f0518b39` — https://agor.sandbox.preset.zone/ui/s/019f16eb93e0734aae9c0791/ (callback enabled, on private-qagor branch). Awaiting verdict.
- **Max (06-30): PR already got a review → DO NOT post to #eng-reviews.** On QAgor PASS, just move branch to zone-human-review ("Ready to stamp!"); skip the channel post. On FAIL, fix loop as usual.
- **QAgor verdict: PASS ✅** (session `019f16eb-93e0-734a-ae9c-0791f0518b39`). Offline SQL-gen verification against real BigQuery dialect (sqlalchemy_bigquery 1.15.0): reproduced bug on master (drill emits `\`forecasts.original\`.\`total_cost\``), confirmed PR fixes it (`\`forecasts\`.\`original\`.\`total_cost\``), regression-checked plain + mixed columns (byte-identical to master), confirmed new branch only triggers for registered dotted string columns (adhoc dicts/unregistered fall through). No live-BigQuery browser pass available, but defect is pure SQL-string gen so the generated SQL is the evidence. Approved & posted QA comment; card → Pass zone; QAgor cleaning up its worktree.
- **Orchestrator actions:** work branch → **zone-human-review** ("Ready to stamp!"). SC → **Reviewing** (500020186). NO #eng-reviews post (per Max). No Slack thread → no reply owed.
- **eschutho review (06-30 01:19): APPROVED** at 844695acb — "Fix is correct — all findings below are nits, none required." 3 take-it-or-leave-it nits (all in the review body, no inline threads): (1) comment attributes collapse to `quote()` but real mechanism is `_process_select_expression`→sqlglot `sanitize_clause` re-serializing the merged identifier so it misses `quoted_columns_by_name` → `literal_column`; (2) note the guard fires for ALL registered physical cols (intentional, plain names identical); (3) note the negative test assertion is a substring of the broken form.
- **Applied all 3** (commit `1376080dd7`, comment/test-comment only, no logic change). Verified the mechanism in code first (_process_select_expression → _process_sql_expression → sanitize_clause). Posted reply on PR thanking her + listing what was applied. New commit re-triggers CI (comment-only, will pass).
- **DONE — awaiting Max's merge.** Branch in zone-human-review, SC Reviewing. Merge patrol will catch the merge → Post-Merge Dance (SC → Merged/Done, branch → zone-done, env stop).
- LESSON: moving a branch to zone-qagor does NOT run QAgor — must create card AND kick off the session. Verify the session actually started, not just that the card exists.

## CI Patrol → QAgor for 3 PRs (2026-06-26 ~20:19)

CI patrol reported ALL 3 zone-auto-review branches green:
- `sc-111745-bigquery-drill-struct` → [PR #41462](https://github.com/apache/superset/pull/41462) ✅
- `sc-111747-tags-favorite-star-fix` → [PR #41460](https://github.com/apache/superset/pull/41460) ✅
- `sc-111747-tags-favorites-filter` → [PR #41461](https://github.com/apache/superset/pull/41461) ✅

Bot feedback check:
- PR #41462: no inline bot comments — clean.
- PR #41460: no inline bot comments — clean.
- PR #41461: bito comment (ID 3483782836) suggesting unit tests for `TagFavoriteFilter`. **Declined** — `DashboardFavoriteFilter`, `SavedQueryFavoriteFilter`, `ChartFavoriteFilter` all lack dedicated unit tests; adding tests only for `TagFavoriteFilter` would be inconsistent. Replied with reason (comment ID 3483898596).

Actions taken:
- All 3 branches moved to **zone-qagor** on Supersetter board.
- SC-111745 → Reviewing (500020186). SC-111747 → Reviewing (500020186) (covers both PRs).
- QAgor cards created in Ready for QA zone (`zone-1774904426448`) on QAgor board:
  - SC-111745 card: `019f0595-74d6-7dea-9b74-9a76b979fafe`
  - SC-111747 (star fix) card: `019f0595-7bf2-7da2-b8ff-c7227e320a9b`
  - SC-111747 (filter) card: `019f0595-81a8-7a7f-a783-8398e9a38b93`
- QAgor sessions created (all with callback to orchestrator `019ed83a-bed0-773b-87fd-443cd73c751f`):
  - SC-111745: `019f0595-fb8c-7603-8709-e68e7031cf09` — https://agor.sandbox.preset.zone/ui/s/019f0595fb8c76038709e68e/
  - SC-111747 star fix: `019f0596-064d-78dd-befa-f986bc5d7c38` — https://agor.sandbox.preset.zone/ui/s/019f0596064d78ddbefaf986/
  - SC-111747 filter: `019f0596-124d-7e4d-b700-d3519131a3a1` — https://agor.sandbox.preset.zone/ui/s/019f0596124d7e4db700d351/

NEXT: wait for QAgor callbacks. On PASS: post #eng-reviews with `<@U0B0BPQV9SQ>`, move to zone-human-review.
