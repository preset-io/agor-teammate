# 2026-06-24 — Deprecation Warning Fix Session

## ~16:05 UTC — Deprecation warning found (Datadog)

**Query:** `deprecat* service:superset env:production`, now-1d / now-7d sweeps.

**Warning chosen:**
```
logger.warning(
    """Your current Slack scopes are missing `channels:read`. Please add
            this to your Slack app in order to continue using the v1 API. Support
            for the old Slack API will be removed in Superset version 6.0.0."""
)
```
Fired from `superset/utils/slack.py:192` in `should_use_v2_api()`. Appearing 5+ times per 90-minute window in production — once per Slack notification send when `ALERT_REPORT_SLACK_V2` feature flag is enabled but `channels:read` scope is missing.

**Root cause:** Triple-quoted Python string written inside an indented `except` block. Python includes the code-level indentation (12 spaces) in the string value, so each continuation line in the log had 12 leading spaces — an artifact of the code's formatting, not the intended message.

**Skipped signals:**
- `helpers.py:1956 Series.__getitem__` FutureWarning — already fixed in master via PR #40936 (Elizabeth, June 11); production image hasn't synced yet
- `authlib.jose` — already fixed PR #41248
- `explore_json` — already fixed PR #41286
- Celery uppercase settings — preset-private config, not fixable in apache/superset
- `appbuilder.app deprecated` — Flask-AppBuilder third-party (base.py)
- `pkg_resources/sqlalchemy_redshift` — third-party

---

## Fix

**Repo:** apache/superset
**Branch:** `fix/slack-deprecation-warning-formatting`
**Commit:** `ddc2492339` — fix(slack): fix indented triple-quoted string in v1 API deprecation warning

**Change:** Replaced triple-quoted string with adjacent string literals. Log output goes from:
```
Your current Slack scopes are missing `channels:read`. Please add
            this to your Slack app in order to continue using the v1 API. Support
            for the old Slack API will be removed in Superset version 6.0.0.
```
To one clean line:
```
Your current Slack scopes are missing `channels:read`. Please add this to your Slack app in order to continue using the v1 API. Support for the old Slack API will be removed in Superset version 6.0.0.
```

Pre-commit hooks: all passed (auto-walrus, mypy, ruff, pylint, ruff-format).

---

## Self-Review

**Subsession:** `019efa64-36b0-7d3f-b6bb-af67f6f4d751`
**URL:** https://agor.sandbox.preset.zone/ui/s/019efa6436b07d3fb6bbaf67/
**Status:** COMPLETE

| # | Item | Verdict |
|---|---|---|
| 1 | Correctness — adjacent literals join with single space, no whitespace artifact | PASS |
| 2 | DRY same file — all other logger calls in slack.py are single-line or %-style | PASS |
| 3 | DRY repo-wide — grep found no other triple-quoted logger message strings | PASS |
| 4 | Test coverage — no existing test for should_use_v2_api(); formatting-only PR has no obligation to add one | NOTE (non-blocking) |
| 5 | Placement — adjacent string literals are the standard Python idiom; textwrap.dedent would be heavier | PASS |

**Overall: APPROVE**

---

## PR

**URL:** https://github.com/apache/superset/pull/41393
**Title:** fix(slack): fix indented triple-quoted string in v1 API deprecation warning
**Commits:** 1 (formatting fix only)

---

## Board Card

**Card ID:** `019efa65-b814-71a4-9571-680672a4e871`
**Zone:** `zone-pr-ready` ("In Review") on SRE board (9a49d11a-6605-4581-b58e-f45fe6bfbadb)

---

## EngCodeReviewBot

**Session ID:** `019efa66-0a25-7146-b294-057825cb03c7`
**Branch:** `019df42c-8d6d-76d2-92bd-a49c169db7f1` (EngCodeReviewBot)
**URL:** https://agor.sandbox.preset.zone/ui/s/019efa660a257146b2940578/
**Status:** Running — asked bot to find a reviewer for the Slack notification subsystem and notify per their preference

---

## Shortcut Story

**ID:** sc-111744
**URL:** https://app.shortcut.com/preset/story/111744
Team: Automations, type: chore, state: Reviewing (500020186)

---

## ~UTC — Requested Changes sweep

Checked `zone-in-review` ("Requested Changes") on SRE board (9a49d11a-6605-4581-b58e-f45fe6bfbadb).

**Result:** No PRs in Requested Changes today. Zone is empty (0 entities). No action taken.

---

## ~22:00 UTC — In Review zone sweep

Checked all 8 cards in `zone-pr-ready` ("In Review") on SRE board.

| PR | Title | Review Decision | Action |
|----|-------|-----------------|--------|
| #41248 | fix(mcp_service): reduce deprecated authlib.jose.errors imports | APPROVED (betodealmeida) | **Merged** + sc-111348 → Merged/Done + card → zone-merged |
| #41267 | fix(deps): restore np.nan in offset_metrics_df empty branch | APPROVED (betodealmeida) — already MERGED before sweep | sc-111351 → Merged/Done + card → zone-merged |
| #41263 | fix(schemas): rename deprecated query fields regardless of falsy values | APPROVED (betodealmeida) but **6 CI checks failing** (test-mysql, test-postgres, test-postgres-required, test-sqlite, unit-tests, unit-tests-required) — cannot merge | No action — card stays in In Review |
| #41286 | fix(views): emit deprecated-endpoint log warning once per endpoint per process | APPROVED (betodealmeida) but **unit-tests-required failing** — cannot merge | No action — card stays in In Review |
| #41002 | fix(viz): use series_limit/series_limit_metric in query_obj dict | REVIEW_REQUIRED — no reviews yet | No action |
| #41025 | fix(pandas_postprocessing): pass string operator names to GroupBy.agg | REVIEW_REQUIRED — codeant-ai COMMENTED only | No action |
| #41096 | fix(reports): pre-commit tab permalinks before state machine transaction | REVIEW_REQUIRED — codeant-ai COMMENTED (2x) | No action |
| #41393 | fix(slack): fix indented triple-quoted string in v1 API deprecation warning | REVIEW_REQUIRED — no reviews yet (opened today) | No action |

**Note on #41263:** 6 required checks failing — likely unrelated flaky tests or merge conflict residue; needs investigation before next sweep.
**Note on #41286:** unit-tests-required failing — may be flaky; worth re-running on next sweep.
**Note on repo:** `--auto` merge is disabled for apache/superset. Merges must be done directly once CI passes.
